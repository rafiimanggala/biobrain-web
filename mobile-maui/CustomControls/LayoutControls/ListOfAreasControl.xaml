<?xml version="1.0" encoding="UTF-8"?>
<ListView xmlns="http://schemas.microsoft.com/dotnet/2021/maui" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              x:Class="CustomControls.LayoutControls.ListOfAreasControl"
              VerticalOptions="Start" HorizontalOptions="FillAndExpand" BackgroundColor="Transparent"
              SeparatorVisibility="None" HasUnevenRows="True">
    <ListView.ItemTemplate>
        <DataTemplate>
            <ViewCell>
                <ViewCell.View>
                    <Grid Padding="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Image Grid.Column="0" Style="{StaticResource ListImageStyle}" Source="{Binding IconPath}" HorizontalOptions="Start"/>
                        <Label Grid.Column="1" Text="{Binding Name}" Style="{StaticResource ListViewLabelStyle}" LineBreakMode="TailTruncation" HorizontalOptions="FillAndExpand"/>
                    </Grid>
                </ViewCell.View>
            </ViewCell>
        </DataTemplate>
    </ListView.ItemTemplate>
</ListView>
